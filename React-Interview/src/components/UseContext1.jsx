import React from 'react'

const UseContext1 = () => {

    // UseContext Hooks helps to manage the state across our application or globally.
    // State or information passing through components, as parent to child components know as props drilling.

    // To avoid prop drilling we use Context API and useContext Hook.   

  return (
    <div>
      
    </div>
  )
}

export default UseContext1
